-- TypeDeclarations.hs
module TypeDeclarations where

import Data.List
import System.IO

addMe :: Int -> Int -> Int

-- functionName param1 param2 = operations (returned value)
addMe x y = x + y
main = do